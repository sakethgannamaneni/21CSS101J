#include<<stdio.h>
void main() {
  int marks, distinction_marks = 75;
  printf("Enter marks :");
  scanf("%d", &marks);
  if (marks > 75) {
    printf("User secured distinction");
    }
    else 
    {
      printf("User did not secured 75");
    }
}
